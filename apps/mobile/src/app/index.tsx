import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default () => {
  return (
    <SafeAreaView>
      <Text className="text-red-500">Layout</Text>
    </SafeAreaView>
  );
};
